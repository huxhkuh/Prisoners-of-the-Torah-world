# Hero image

Save the supplied hero image in this folder as:

`hero-prisoner.png`

The stylesheet already points to `assets/images/hero-prisoner.png`. Until that PNG is present, the site uses `hero-prisoner.svg` as a fallback in the same mood and layout.
